# Azure Configuration
AZURE_TENANT_ID=
AZURE_SUBSCRIPTION_ID=your_subscription_id
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=

# Key Vault Configuration
AZURE_KEY_VAULT_URL=https://your-keyvault.vault.azure.net/
AZURE_KEY_VAULT_NAME=your-keyvault-name

# Teams Bot Configuration (to be moved to Key Vault)
MICROSOFT_APP_ID=your_app_id
MICROSOFT_APP_PASSWORD=your_app_password

# Cosmos DB Configuration (to be moved to Key Vault)
COSMOS_DB_ENDPOINT=
COSMOS_DB_KEY=
COSMOS_DB_DATABASE=teams_bot_db
COSMOS_DB_CONTAINER=state

# Snowflake Configuration (to be moved to Key Vault)
SNOWFLAKE_ACCOUNT=
SNOWFLAKE_PRIVATE_KEY_PATH=teams_bot/config/keys/snowflake_bot_key.pem

# State Management Configuration (to be moved to Key Vault)
STATE_ENCRYPTION_KEY=

# Logging Configuration
LOG_LEVEL=INFO
LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s

# Development Configuration
STATE_STORAGE_TYPE=memory  # Use 'memory' for development, 'cosmos' for production 