"""# Cron job for secret rotation
# Run daily at 2 AM UTC
0 2 * * * cd /path/to/teams_bot && python3 scripts/rotate_secrets.py >> /var/log/teams_bot/rotation.log 2>&1

# Check secret expiry daily at 1 AM UTC
0 1 * * * cd /path/to/teams_bot && python3 scripts/check_secret_expiry.py >> /var/log/teams_bot/expiry_check.log 2>&1"""
